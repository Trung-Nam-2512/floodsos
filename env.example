# Backend
PORT=5000

# MongoDB Connection String
# Local: mongodb://localhost:27017/cuu-ho-lu-lut
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/cuu-ho-lu-lut
MONGODB_URI=mongodb://localhost:27017/cuu-ho-lu-lut

# OpenAI API Key (lấy tại https://platform.openai.com/api-keys)

# Frontend - Mapbox Token (lấy tại https://account.mapbox.com/)
# Vite sử dụng prefix VITE_ cho biến môi trường
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# Geocoding: Dùng OpenStreetMap Nominatim (FREE - không cần API key, không cần thẻ)
# Rate limit: 1 request/second (tự động delay trong code)

# Mapbox Token (optional - backup nếu Nominatim fail)
# Có thể dùng cùng token với frontend
MAPBOX_TOKEN=your_mapbox_token_here

# Google Geocoding API (optional - chỉ dùng nếu có API key và thẻ)
# Lấy tại https://console.cloud.google.com/apis/credentials
GOOGLE_GEOCODING_API_KEY=your_google_geocoding_api_key_here

# API URL (để trống nếu chạy local)
VITE_API_URL=http://localhost:5000

# Hoặc dùng REACT_APP_ prefix (hỗ trợ cả hai)
REACT_APP_MAPBOX_TOKEN=your_mapbox_token_here
REACT_APP_API_URL=http://localhost:5000
